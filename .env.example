## Frontend runtime config
# For dev, leave empty (uses Vite proxy).
# For production with separate backend origin, set to your backend base URL.
# Example: VITE_API_BASE=https://your-backend.example.com
VITE_API_BASE=
